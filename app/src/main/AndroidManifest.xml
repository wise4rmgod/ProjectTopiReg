<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.multikskills.projecttopireg">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Mainpage_Activity"
            android:label="@string/title_activity_mainpage_"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddProject_topic" />
        <activity
            android:name=".Admin_Activity"
            android:label="@string/title_activity_admin_"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AcceptedStudents_Activity" />
        <activity android:name=".AdminAcceptAndReject_Activivty" />
        <activity android:name=".PendingStudents" />
        <activity android:name=".AcceptedStudents"></activity>
    </application>

</manifest>